export default class MenuItem {
  constructor(name, description, image, stats) {
    this.name = name;
    this.description = description;
    this.image = image;
    this.stats = stats;
  }

  // other methods to manipulate a menu item
}
